/**
 * Board lifecycle: mount/teardown is implemented in {@link mountWorkbenchPrimaryBoard}
 * from `./mountWorkbenchBoard` (used by WorkbenchView effects).
 */
export { mountWorkbenchPrimaryBoard, attachPinchZoomWheel } from "./mountWorkbenchBoard";

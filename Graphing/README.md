# Graphing_Compoment

Next.js app: JSXGraph canvas, formula entry (MathLive / Editor.js), and chart helpers. See `LICENSE` (MIT).

```bash
npm install
npm run dev
```

Static export: `npm run build` then serve the `out` directory.
